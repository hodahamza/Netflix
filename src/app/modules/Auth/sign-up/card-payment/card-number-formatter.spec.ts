import { CardNumberFormatter } from './card-number-formatter';

describe('CardNumberFormatter', () => {
  it('should create an instance', () => {
    const directive = new CardNumberFormatter();
    expect(directive).toBeTruthy();
  });
});
